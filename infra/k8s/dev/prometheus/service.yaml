# Apply: kubectl apply -R -f infra/k8s/dev
apiVersion: v1
kind: Service
metadata:
  name: prometheus-svc
  namespace: dev
spec:
  selector:
    app: prometheus
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 9090
