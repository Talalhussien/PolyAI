# Apply: kubectl apply -R -f infra/k8s/dev
apiVersion: v1
kind: Service
metadata:
  name: grafana-svc
  namespace: dev
spec:
  selector:
    app: grafana
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3000
