# Apply: kubectl apply -R -f infra/k8s/dev
apiVersion: v1
kind: Service
metadata:
  name: agent-svc
  namespace: dev
  labels:
    app: agent
spec:
  selector:
    app: agent
  ports:
    - protocol: TCP
      name: http
      port: 8000
      targetPort: 8000
