# Apply: kubectl apply -R -f infra/k8s/prod
apiVersion: v1
kind: Service
metadata:
  name: prometheus-svc
  namespace: prod
spec:
  selector:
    app: prometheus
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 9090
