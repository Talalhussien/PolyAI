# Apply: kubectl apply -R -f infra/k8s/prod
apiVersion: v1
kind: Service
metadata:
  name: agent-svc
  namespace: prod
spec:
  selector:
    app: agent
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
