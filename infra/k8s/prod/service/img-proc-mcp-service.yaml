# Apply: kubectl apply -R -f infra/k8s/prod
apiVersion: v1
kind: Service
metadata:
  name: img-proc-mcp-svc
  namespace: prod
spec:
  selector:
    app: img-proc-mcp
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
